# ShelfSum module and invariant map

Use this map to find the owner of a boundary. Keep interfaces small: callers provide the Business, actor, validated intent, and required record; the owning module hides transaction, audit, and persistence detail.

## Invariants

| ID | Invariant | Primary owner | Proof expectation | Revisit when |
| --- | --- | --- | --- | --- |
| INV-01 | Resolve every Business-owned identifier only inside the authenticated membership's Business. | `businesses.access` plus owning query/service | Cross-Business request and service tests | Any Business-owned route or service is added |
| INV-02 | Stock on Hand equals the net effect of immutable Stock Movements. | `inventory` | Reconciliation and mutation-bypass tests | Any stock-affecting workflow changes |
| INV-03 | Purchase, Sale, adjustment, reversal, stock, movement, and audit effects succeed or fail atomically. | Owning application service | Success plus downstream-failure rollback tests | A multi-record workflow changes |
| INV-04 | Completed stock-affecting records remain immutable; correction uses an explicit reversal. | Owning domain model/queryset/service | Direct, bulk, repeated-action, and reversal tests | Status or correction behaviour changes |
| INV-05 | Demo Business writes are rejected at both request and service boundaries. | `businesses.rules` plus every mutating entry point | Crafted-request and direct-service tests | Any write path is added |
| INV-06 | Money uses decimal arithmetic; first-release quantities use whole units. | Forms, models, and services | Boundary, precision, and invalid-input tests | Money or quantity representation changes |

## Interfaces

| ID | Caller -> owner | Contract | Preconditions | Failure proof | Current anchors |
| --- | --- | --- | --- | --- | --- |
| IF-01 | protected view -> `businesses.access` | Establish active membership and `request.business` before object lookup. | Authenticated active member | Stable redirect/403 and isolation tests | `businesses/access.py` |
| IF-02 | mutating view/service -> `businesses.rules` | Reject Demo Business and unauthorized or inactive actors. | Business and actor are explicit | Request and direct-service denial tests | `businesses/rules.py` |
| IF-03 | `catalogue.create_product` -> internal inventory opening path | Record opening stock during Product creation; the public manual-adjustment interface cannot enable an opening override. | Newly created matching Business and Product; valid whole quantity | Atomic rollback, no negative stock, immutable trace | `catalogue/services.py`, `inventory/services.py` |
| IF-04 | domain service -> `auditing` | Record each consequential change inside the same transaction. | A valid changed object and actor | Audit failure rolls back the whole operation | `auditing/services.py` |
| IF-05 | catalogue view -> Product queryset | List, lookup, search, and stock-activity availability remain Business-scoped. | Current Business is explicit | Cross-Business records never appear | `catalogue/models.py`, `catalogue/views.py` |
| IF-06 | Owner membership views -> `businesses.services` | Add or deactivate Staff Members atomically with an Audit Event while preserving one-Business membership. | Active Owner, current Business, registered unassigned target | Owner, Demo, duplicate, assignment, and cross-Business denial tests | `businesses/services.py`, `businesses/views.py` |
| IF-07 | purchase views -> `purchases.complete_purchase` | Complete a draft Purchase atomically across locked Products, Stock on Hand, current unit cost, immutable Purchase-origin Stock Movements, status, and Audit Event. | Active member, writable Business, scoped draft Purchase, valid active Product lines | Request/service isolation, denial, rollback, replay, provenance, and reconciliation tests | `purchases/services.py`, `purchases/views.py`, `inventory/models.py` |
| IF-08 | sale views -> `sales.complete_sale` | Complete a draft Sale atomically across deterministically locked Products, availability recheck, cost snapshots, Stock on Hand, immutable Sale-origin Stock Movements, status, and Audit Event. | Active member, writable Business, scoped draft Sale, valid active Product lines within Stock on Hand | Request/service isolation, denial, rollback, replay, provenance, immutability, and reconciliation tests | `sales/services.py`, `sales/views.py`, `inventory/models.py` |
| IF-09 | void confirmation views -> `purchases.void_purchase` / `sales.void_sale` | Void a completed document once using exact immutable reversals, stock updates, protected VOIDED status, and Audit Event in one transaction. | Active member, writable Business, scoped completed document, intact origin history; sufficient Purchase stock | Forgery, replay, negative stock, CSRF, isolation, rollback, and reconciliation tests | `purchases/services.py`, `sales/services.py`, `inventory/models.py` |
| IF-10 | stock adjustment views -> `inventory.record_stock_adjustment` | Preview a signed whole-unit correction, then recheck a locked active Product and atomically persist the Stock Adjustment, Stock Movement, Stock on Hand, and `stock.adjusted` Audit Event. | Active member, writable Business, scoped active Product, supported non-opening reason | Request/service isolation, CSRF, preview no-write, negative/stale recheck, rollback, reconciliation, immutable/provenance tests | `inventory/forms.py`, `inventory/views.py`, `inventory/services.py`, `inventory/models.py` |
| IF-11 | Business home view -> `businesses.dashboard_context` | Compose the full current Africa/Lagos calendar month and current stock balance from scoped recorded facts, with links to explanatory records. | Active membership and explicit current Business | Known-value reconciliation, inclusive date boundaries, role access, terminal-record exclusion, and cross-Business tests | `businesses/dashboard.py`, `businesses/views.py`, `businesses/tests/test_dashboard.py` |
| IF-12 | report presenters -> `reports.sales.build_sales_report` | Return one Business-scoped, inclusive-filtered Sales result with completion-time cost estimates and active completed-Sale totals for both HTML and CSV. | Active membership establishes the current Business; completed or voided status and valid date range | Request parity, formula-safety, access, invalid-range, and isolation tests | `reports/sales.py`, `reports/views.py`, `reports/tests/test_sales_report.py` |
| IF-13 | report presenters -> `reports.purchase.build_purchase_report` | Return one Business-scoped, inclusive-filtered Purchase result with quantity-by-unit-cost totals and active completed-Purchase totals for both HTML and CSV. | Active membership establishes the current Business; completed or voided status and valid date range | Request parity, formula-safety, access, invalid-range, and isolation tests | `reports/purchase.py`, `reports/views.py`, `reports/tests/test_purchase_report.py` |
| IF-14 | report presenters -> `reports.expense.build_expense_report` | Return one Business-scoped, inclusive-filtered Expense result with recorded-only active totals for both HTML and CSV. | Active membership establishes the current Business; recorded or voided status and valid date range | Request parity, correction semantics, formula-safety, access, invalid-range, and isolation tests | `reports/expense.py`, `reports/views.py`, `reports/tests/test_expense_report.py` |
| IF-15 | report presenters -> `reports.stock_position.build_stock_position_report` | Return one current, Business-scoped Product snapshot for both HTML and CSV, with Product-status and stock-state filters and an exact displayed-row total. | Active membership establishes the current Business; valid Product-status and stock-state filters | Request parity, dashboard reconciliation, formula-safety, access, invalid-filter, and isolation tests | `reports/stock_position.py`, `reports/views.py`, `reports/tests/test_stock_position_report.py` |
| IF-16 | report presenters -> `reports.movement.build_movement_report` | Return one Business-scoped, Lagos-local inclusive-filtered Stock Movement result for both HTML and CSV, with kind/Product filters, signed net change, and persisted origin links. | Active membership establishes the current Business; valid date, kind, and current-Business Product filters | Request parity, UTC/Lagos boundaries, origin/reversal links, signed totals, formula-safety, access, invalid-filter, and isolation tests | `reports/movement.py`, `reports/views.py`, `reports/tests/test_movement_report.py` |
| IF-17 | protected Audit Event browser -> `auditing.build_audit_event_browser` | Return one Business-scoped, newest-first Audit Event result with actor/action and Lagos-local inclusive date filters. | Active membership establishes the current Business; actor and action choices derive only from that Business's Audit Events. | Request isolation, deactivated-actor retention, Lagos boundaries, invalid filters, and access tests | `auditing/browser.py`, `auditing/views.py`, `auditing/tests/test_audit_event_browser.py` |
| IF-18 | Business mutation views -> `businesses.services` | Create a Business with its active Owner membership, or update validated name, phone, and address settings, atomically with attributed Audit Events. | Authenticated actor; creation has no existing membership; settings require an active Owner of the current writable Business | Creation duplicate/race behavior, Owner/Demo/Staff/inactive/foreign denial, no-op audit suppression, and audit-failure rollback tests | `businesses/services.py`, `businesses/views.py` |
| IF-19 | Purchase/Sale creation views -> owning draft services | Create a draft Purchase or Sale from validated parent and line intent, assigning the explicit Business and actor and persisting parent plus lines atomically without an Audit Event. | Active member, writable Business, at least one non-deleted line, and active Products belonging to the explicit Business | Request/service Demo and membership denial, explicit actor/Business attribution, cross-Business Product rejection, no draft Audit Event, and line-persistence rollback tests | `purchases/services.py`, `purchases/views.py`, `sales/services.py`, `sales/views.py` |
| IF-20 | `seed_demo` management command -> `core.demo` | Create the named fictional Demo Business and canonical multi-week records atomically, or verify the exact logical manifest on rerun before resetting only its named credentials. | No canonical state for first run, or one complete Demo Business with both named users and `is_demo=True` for rerun | Partial/drifted state fails without credential changes; downstream failures roll back all first-run rows; other Businesses and users remain untouched; committed Business is Demo | `core/demo.py`, `core/management/commands/seed_demo.py` |

Expenses are owned by `expenses`; recording and explicit void-and-replace correction are transactional services, and every lookup is membership-scoped by its views.

Draft Purchase and Sale edits use their owning `save_draft_purchase` and `save_draft_sale` services. Editing, completion, and voiding acquire the parent document before ordered lines and recheck lifecycle state after locking.

Update this file only when ownership, a public seam, or an invariant changes. Implementation detail that does not affect callers belongs in code and tests.
