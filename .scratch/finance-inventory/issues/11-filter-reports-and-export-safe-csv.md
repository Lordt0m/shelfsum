# 11: Filter reports and export safe CSV

**What to build:** Let Business users inspect date-filtered Sales, Purchases, Expenses, stock position, and Product movement reports and export exactly the active result set as spreadsheet-safe CSV.

**Blocked by:** 06: Complete Purchases atomically; 07: Complete Sales with availability protection; 09: Record Expenses and Stock Adjustments.

**Status:** ready-for-agent

- [ ] Each report has a clear purpose, useful columns, totals where meaningful, and inclusive start/end filters preserved in the URL.
- [ ] Completed, voided, and active-state rules match the specification and dashboard formulas.
- [ ] CSV exports reuse the report query and filters and expose explicit stable headings.
- [ ] Exported text beginning with `=`, `+`, `-`, or `@` is neutralized against spreadsheet formula execution.
- [ ] Naira and decimal values remain machine-readable without losing precision.
- [ ] Empty, invalid-range, and cross-Business requests behave safely and clearly.
- [ ] Tests cover filters, boundaries, totals, headers, encoding, injection protection, permissions, and isolation.

## Comments

- Sales report slice accepted at `5de1c1d`, following `acb4416`. One Business-scoped result feeds the HTML and UTF-8 CSV presenters, defaults to the full current Africa/Lagos calendar month, preserves explicit one-sided or inclusive date filters, and exposes completed or voided history without including voided rows in active totals.
- Sales CSV uses stable headings, ISO dates, two-place machine-readable decimals, and apostrophe-prefixes formula-leading text. Twelve focused tests and the 161-test suite passed with one PostgreSQL-only concurrency test skipped on SQLite; fresh Standards and Spec reviews passed after default-period and malformed-date repairs.
- Ticket 11 remains open. Purchases, Expenses, stock position, and Product movement reports must reuse the proven filter/export boundary before completion.
- **Next slice contract - Purchases:** default to completed Purchases in the full current Africa/Lagos calendar month; allow explicit completed or voided inspection and explicit one-sided or inclusive date filters. Show Purchase date, immutable Purchase reference/ID link, supplier, status, and quantity-by-unit-cost total. Sum only displayed completed Purchase cost into the active total; retain voided row amounts for explanation while labelling them excluded from active totals. The CSV must use the identical scoped result, stable text and decimal columns, and the same formula-leading-text defence for supplier and reference values.
- The Purchase slice may extract only already-proven cross-report mechanics, especially CSV text neutralization and date-range parsing. Keep status rules, row meaning, and totals owned by each report so a generic abstraction cannot blur Sale revenue/COGS with Purchase cost. Prove Lagos defaults and UTC boundary, inclusive and one-sided dates, malformed/reversed filters, completed/voided behavior, HTML/CSV parity, formula safety, both roles, Demo reads, inactive/anonymous denial, Business isolation, and empty results before review.
- **Following slice contract - Expenses:** default to recorded Expenses in the full current Africa/Lagos calendar month; allow explicit recorded or voided history and explicit one-sided or inclusive Expense-date filters. Show date, immutable Expense ID link, category, description, status, and amount. Sum only displayed recorded amounts; a voided original contributes nothing while its recorded replacement contributes once. Apply the shared CSV defence to every text column and prove correction, status, date, parity, access, isolation, empty, and invalid cases.
- **Following slice contract - current stock position:** this is a current snapshot, not a historical valuation, so date filters do not apply. Default to every active or inactive Product with positive Stock on Hand so the unfiltered total reconciles the dashboard formula. Allow explicit active/inactive/all Product status and positive/low/zero/all stock-state filters. Show Product name and SKU with a Product-detail link, active state, Stock on Hand, current unit cost, and quantity-by-current-cost value; sum exactly the displayed rows. State that current costs do not reconstruct historical inventory value. CSV text safety, Decimal precision, Business isolation, filter parity, zero/inactive behavior, and dashboard reconciliation are required.
- **Following slice contract - Product movements:** default to the full current Africa/Lagos calendar month using movement creation time converted at Lagos boundaries. Allow explicit one-sided or inclusive dates, movement kind, and a Product selected only from the current Business. Show Lagos timestamp, Product and SKU with Product-detail link, kind, signed whole-unit change, and an explanatory origin link for adjustment, Purchase, Sale, or reversal. The displayed net change is the signed sum of exactly the filtered movements. CSV uses the same rows, ISO timestamp with offset, stable headings, integer quantities, and formula-safe text. Prove local-time boundaries, every origin kind including reversal, scoped Product selection, parity, access, isolation, empty, and invalid filters.
- Shared code may own validated date ranges, Lagos datetime boundaries, CSV response construction, and text neutralization only after at least two reports need them. Each report module continues to own its queryset, allowed statuses/kinds, row meaning, totals, and explanatory links. No report may infer historical stock valuation from the current Product cost.
- Purchase report slice accepted at `633bc13`, following implementation `850ad35`. It reuses only shared inclusive-date and CSV-response mechanics while keeping Purchase statuses, row meaning, supplier fallback, quantity-by-unit-cost totals, and completed-only active totals in `reports.purchase`. The global Reports entry now opens a directory that exposes both accepted reports.
- Purchase and Sales focused verification passed 24 tests; the complete suite passed 173 tests with one expected PostgreSQL-only concurrency skip on SQLite. Django checks, migration drift, and whitespace checks passed. Fresh Standards and Spec re-reviews passed with no required findings after repairing report discovery and whitespace-prefixed CSV formula defence. Ticket 11 remains open for Expenses, current stock position, and Product movements.
- Expense report slice accepted at `28ca492`, following implementation `0e64f9c`. The Business-scoped recorded/voided result is shared by HTML and CSV, defaults to the full current Africa/Lagos month, preserves inclusive and explicit one-sided filters, counts a recorded correction replacement once while excluding its voided original, and exposes accessible table structure plus spreadsheet-safe text.
- Focused Sales, Purchase, and Expense verification passed 36 tests; the complete suite passed 185 tests with one expected PostgreSQL-only concurrency skip on SQLite. Django checks, migration drift, and whitespace checks passed. Fresh Standards and Spec re-reviews passed with no findings after the Expense table caption/header-scope repair. Ticket 11 remains open for current stock position and Product movements.
