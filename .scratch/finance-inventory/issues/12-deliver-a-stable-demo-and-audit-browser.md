# 12: Deliver a stable demo and Audit Event browser

**What to build:** Provide a deterministic fictional Demo Business, enforce read-only demo access, and let authorized Business users inspect the Audit Events produced by every consequential workflow.

**Blocked by:** 04: Maintain and find Products; 05: Add Staff Members with enforced permissions; 08: Void completed stock documents; 09: Record Expenses and Stock Adjustments; 10: Show an explainable Business dashboard; 11: Filter reports and export safe CSV.

**Status:** in-progress

- [ ] A deterministic command recreates a fictional Business with Owner and Staff Member demo users, several weeks of records, and at least one low-stock Product.
- [ ] Demo credentials are documented and visible from the landing page without exposing real personal or Business data.
- [ ] Demo users can inspect every major workflow and report.
- [ ] Every write attempt against the Demo Business is rejected by shared server-side policy with a clear response.
- [ ] Audit Events exist for every consequential action named in the specification and remain append-only through the application.
- [ ] The Audit Event browser filters by actor, action, and date and remains scoped to the current Business.
- [ ] Tests cover deterministic seeding, idempotent recreation, read-only enforcement at request and service seams, audit attribution, and isolation.

## Comments

- **2026-09-16 inventory at `cd38281`:** the repository has no demo seed command, fixture, credentials, or Audit Event browser. The landing page and README still describe demo access as future work. Existing dashboard and report reads are Business-scoped and Demo-compatible, but a seeded demo must prove every major read workflow against coherent fictional data.
- Shared Demo rejection already exists at `businesses.rules.ensure_business_write_allowed` and the request decorator. Product, membership, Purchase completion/edit/void, Sale completion/edit/void, Expense, and Stock Adjustment services use it with direct-service tests. Business settings plus Purchase and Sale draft creation still persist directly in views, so those mutation entry points have request-only Demo protection and must gain guarded service seams before the ticket can claim complete request-and-service enforcement.
- Existing append-only application events cover Product create/update/deactivate, Staff Member add/deactivate, Purchase complete/void, Sale complete/void, Expense record/void/correct, and manual Stock Adjustment. These are the consequential business, permission, stock, and money lifecycle actions already named by the specification and earlier ticket contracts. Draft Purchase/Sale create/edit, sign-in/password actions, and read activity are deliberately excluded from the audit contract because they do not finalize a stock, money, permission, or immutable business fact. Business creation and Business settings changes are consequential configuration actions and must gain attributed `business.created` and `business.updated` events.
- `AuditEvent` is append-only through the application model/queryset boundary, retains deactivated actors through protected foreign keys, and is already used for the dashboard's recent activity. Ticket 12 does not claim database-trigger-level immutability. The complete browser must retain events from deactivated actors and scope actor choices and results to the current Business.
- Repository-wide guidance remains accurate after this inventory; no `AGENTS.md`, ADR, or module-map ownership change is needed until a new public service/query seam is implemented.

### Ordered slice contracts

1. **Read-only Audit Event browser:** add one Business-scoped query/presenter owned by `auditing`, a membership-protected GET route and accessible template, stable newest-first ordering, actor/action and explicit one-sided or inclusive Africa/Lagos date filters, raw invalid-value preservation, clear empty/error states, and navigation from the dashboard. Prove Owner, active Staff, and Demo reads; inactive/anonymous denial; deactivated-actor filtering; cross-Business isolation in results and choices; and local datetime boundaries. Do not add seed data or change event-production semantics in this slice.
2. **Mutation-boundary and audit completion:** introduce transactional service seams for Business creation/settings and draft Purchase/Sale creation. Apply the shared Demo rule wherever an existing Business is mutated. Emit `business.created` and `business.updated` atomically with their changes; keep draft creation/editing outside the event catalog. Prove request and direct-service denial, attribution, isolation, and audit-failure rollback. Update the module map only for the new public seams.
3. **Deterministic fictional demo:** add an idempotent management command that recreates only explicitly identified demo users and one Demo Business. Build coherent several-week data through owning services where possible: active/inactive and low-stock Products, completed and voided Purchases/Sales, an Expense correction, manual adjustment, Stock Movements, dashboard totals, reports, and Audit Events. Because normal Demo writes must remain impossible and existing history is append-only, the command may use an explicit atomic maintenance mode scoped only to the named fictional Business: verify an already-canonical dataset as a no-op, or stage the named Business as writable while filling missing canonical records, then restore `is_demo=True` before commit. It must fail and roll back on unexpected drift rather than deleting immutable history or broadening the runtime write rule. Re-running must converge to the same logical dataset, reset only the named fictional credentials, and never touch non-demo Businesses or rely on wall-clock time, paid APIs, or an AI model.
4. **Public demo presentation and closure proof:** publish fictional credentials and a clear read-only message on the landing page and README; prove both demo roles can inspect every major workflow/report and the Audit browser; run a consolidated mutation matrix across every mutating request and every public mutating service; then complete full checks, independent Standards/Spec review, and the ticket completion record.

- **Next safe action:** implement slice 1 test-first from `cd38281`. Begin with a failing request test for a current-Business audit list that includes a deactivated actor and excludes an in-range foreign-Business event; then add actor/action/Lagos-date filters without introducing a generic cross-report abstraction.
- **Audit browser slice accepted at `9e5ddad`, following implementation `2085dce`:** active Owners, Staff Members, and Demo members can browse newest-first current-Business Audit Events and filter by retained actor, action, and explicit one-sided or inclusive Africa/Lagos dates. Invalid filters preserve the submitted value, disclose no rows, and offer no foreign-Business choices. The accessible server-rendered table is linked from global navigation and the dashboard.
- Red began with the missing `audit_event_list` route. Six focused browser tests, 33 related browser/dashboard/access tests, and the normally discovered 213-test suite passed; one expected PostgreSQL-only concurrency test was skipped on SQLite. Django checks, migration drift, and whitespace checks passed. Fresh Standards and Spec re-reviews passed with zero findings after adding the missing test-package marker plus explicit Demo Staff and deactivated-actor filter proofs.
- **Next safe action:** implement ordered slice 2 from `9e5ddad`, starting with transactional Business creation/settings services and their attributed `business.created` / `business.updated` events. Preserve the established request behavior, then add guarded draft Purchase/Sale creation seams without adding draft lifecycle Audit Events.
- **Business lifecycle audit slice accepted at `03a24da`, following implementation `fa93607`:** Business creation and settings now use transactional services. Creation serializes on the actor, creates one active Owner membership, and emits attributed `business.created`; settings require an active Owner of a writable Business, audit only actual changes as `business.updated`, and keep phone/address values out of the summary. Mutation and Audit Event creation roll back together.
- Red began with the creation request producing no Audit Event. The first independent Standards review found a broad `IntegrityError` handler that could misreport an audit persistence failure and missing real-database race proof. The repair removed that handler, added a request-level audit-integrity rollback regression, and added a PostgreSQL-only two-worker creation race proving exactly one Business, one active Owner membership, one creation event, and one stable duplicate error. Final fresh Standards and Spec reviews passed with zero findings.
- Thirty-two focused Business/Audit-browser tests and the complete 221-test suite passed locally; two PostgreSQL-only concurrency tests were intentionally skipped on SQLite. Django checks, migration drift, and whitespace checks passed. The new Business-creation race test remains a release-environment check that must execute against PostgreSQL.
- **Next safe action:** continue ordered slice 2 from `03a24da` by introducing guarded transactional draft Purchase and Sale creation services. Preserve existing forms, redirects, attribution, and Business isolation; reject Demo writes at direct-service and request seams; keep drafts outside the Audit Event catalog; and prove rollback plus foreign-Business denial before fresh Standards/Spec review.
